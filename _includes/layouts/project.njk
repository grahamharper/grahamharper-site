---
layout: layouts/base.njk
---

{# Only include the syntax highlighter CSS on blog posts #}
{# {%- css %}{% include "node_modules/prism-themes/themes/prism-coldark-dark.css" %}{% endcss %} #}
{# {%- css %}{% include "public/css/prism-diff.css" %}{%- endcss %} #}
    <article class="post flow"> <h1>{{ title }}</h1>
    {# <ul class="post-metadata">
                {%- for tag in tags | filterTagList %}
                    {%- set tagUrl %}/tags/{{ tag | slugify }}/{% endset %}
                    <li>
                        <a href="{{ tagUrl }}" class="post-tag">{{ tag }}</a>
                        {%- if not loop.last %},
                        {% endif %}
                    </li>
                {%- endfor %}
            </ul> #}
    {{ content | safe }}
    {%- if collections.work %}
        {%- set previousPost = collections.work | getPreviousCollectionItem %}
        {%- set nextPost = collections.work | getNextCollectionItem %}
        {%- if nextPost or previousPost %}
            {% include "../previousnext.njk" %}
        {%- endif %}
    {%- endif %}
</article>